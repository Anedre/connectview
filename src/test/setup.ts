// Extiende `expect` con los matchers de jest-dom (toBeInTheDocument, etc.) para
// los tests de componentes con Testing Library. Se carga vía vitest setupFiles.
import "@testing-library/jest-dom/vitest";
