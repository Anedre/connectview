/** Tonos válidos del <Pill> de ARIA (espejo del union interno de
 *  primitives.tsx, que no lo exporta). Se usa para tipar las tablas
 *  de prioridad/estado del cockpit sin castear a any. */
export type PillTone =
  | "accent"
  | "green"
  | "gold"
  | "red"
  | "coral"
  | "iris"
  | "cyan"
  | "outline";
