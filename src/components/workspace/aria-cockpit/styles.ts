import type { CSSProperties } from "react";

/** Estilo de input compartido en el modo demo (marcador, crear lead). */
export const INP_STYLE: CSSProperties = {
  width: "100%",
  border: "1px solid var(--border-1)",
  borderRadius: "var(--r-md)",
  padding: "10px 12px",
  background: "var(--bg-2)",
  fontSize: 13,
  outline: "none",
};
