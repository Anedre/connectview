# Sesión 2026-06-10 — Resumen + Pendientes (handoff)

> Qué se hizo: **Rediseño Fase 1 completa** (sistema: colores→tokens, confirm()→ConfirmDialog,
> marca AIRA, a11y nav, emoji→lucide, código muerto) · **Dashboard ejecutivo premium**
> (recreación del diseño Claude Design v2, ECharts, paleta diversa aprobada, datos reales,
> `/inicio-demo`) · **Reportes premium** (mismo patrón, recharts DESINSTALADO — P6 100%) ·
> **#15 Automatizaciones** (motor completo + UI `/automations`, e2e verificado) ·
> **#10 WhatsApp Flows v1** (envío + captura nfm_reply + trigger de automatización, e2e sintético).
> ~30 commits, todo `tsc`/`build` 0.

## PENDIENTES

### A · Pasos del USUARIO (rápidos)
1. **#10 e2e real:** crear/publicar un Flow en Meta Business Manager → WhatsApp Manager → Flows,
   y pegar su `flow_id` en Configuración → Integraciones → WhatsApp → Formularios.
2. (Solo números Meta aparte) registrar el webhook en la app de Meta: callback
   `https://uunzfcsebawcjtppv2n3kklc340qeofb.lambda-url.us-east-1.on.aws/` + verify token
   `aria-wa-b8b8564c91e157c421ea4473`, suscribir `messages`.
3. Borrar el Lead de prueba en la SF dev org: **"E2E Prueba (borrar)"** (`00QgL00000U9w9xUAB`)
   — el `sf` CLI quedó sin auth en esta máquina (`sf org login web -a voxdev` lo re-habilita).

### B · Features del UNIFY_ROADMAP no construidas
| # | Feature | Esfuerzo | Nota |
|---|---|---|---|
| 5 | Reportes Chattigo restantes (sesiones/bot/chat-CRM) | L | a varios les falta fuente de datos |
| 7 | Exports programados (cron + XLSX + SES) | M | |
| 11 | Plantillas Carousel + List en campañas | M | necesita envío real para verificar |
| 13 | Quality Rating / límites del número WA | M | widget + alarma |
| 16 | Bot al WhatsApp REAL (producción) | M | wiring del contact flow: Get customer input → invoke `bot-runtime` (inbound-safe) |
| 17 | Webhooks salientes con retry multi-día | M | SQS+DLQ; hoy #15 hace 1 intento |
| 21 | Auto-resumen post-conversación | S | quick win — Bedrock ya cableado |
| 22 | Sentiment-coaching al supervisor (tiempo real) | M-L | camino B: stream Contact Lens → DDB TTL |
| 24 | Canal Mercado Libre | L | moat LATAM |
| 27 | SSO SAML/OIDC | M | federación en Cognito |
| 18 | Reactivación | — | **ya cubierto por #15** (plantilla "Reactivación 7d") — marcar ✅ |

### C · Extensiones v1→v2 de lo construido HOY
- **#15 Automatizaciones:** `lead_created` no dispara desde carga CSV de campañas ni SF-inbound;
  `schedule_callback` solo tenant legacy (dispatcher pooled); reglas no se encadenan (deliberado);
  env de `save-agent-notes` puesto por CLI = **drift en el próximo `ampx` deploy** (pendiente
  `addEnvironment` en `amplify/backend.ts`).
- **#10 Flows:** Flows en campañas masivas (template con botón FLOW); captura estructurada para
  modo "aws" (hoy la respuesta cae al chat del agente — un solo event destination por WABA);
  listado automático de Flows vía Graph API (hoy el flow_id se pega a mano).

### D · Rediseño (design/) — Fases 2-3 del brief
- **Pulir Grabaciones** — la última pantalla 🟠: AudioPlayer accesible con **waveform** +
  marcadores de sentiment, transcript clickable-to-seek, hilo omnicanal unificado
  (`ConversationCanvas`). La oportunidad de diseño #1 según la auditoría.
- **Vista de AGENTE del Inicio** (el handoff v2 también la trae; solo se recreó la ejecutiva).
- Primitivos faltantes: `DataTable`, `ChannelBadge`, `StatusDot/SeverityBadge`, `Avatar`,
  `CallDuration`, `SegmentedControl` global, `FilterChipBar`, `FreshnessPill`.
- **Huérfanos a borrar:** `dashboard/StatCard.tsx`, `dashboard/CustomCharts.tsx`,
  `dashboard/DashboardControls.tsx` (0 usos tras el dashboard nuevo), `ExecFunnelEChart`
  (quedó sin uso al elegir barras). Decisión pendiente: Gamification/Churn/Wellness cards
  (premium pero sin montar — ya tokenizadas).
- Responsive global (P10), paleta de datos en light theme, `aria-live` exhaustivo en pantallas
  live, command palette del agente, CSAT real (hoy es proxy de % sentiment positivo).

### E · SaaS multi-tenant (de SAAS_MIGRATION_STATE.md, sin cambios)
- EventBridge discovery scan per-tenant (dialer/callback-dispatcher para BYO real).
- `process-contact-event` reverse lookup (GSI instanceArn→tenantId).
- ~15 Lambdas restantes de migración BYO; rate-limit de `verify-connect-connection` (bajo).

## Recomendación de arranque próxima sesión
1. Quick wins: **#21 auto-resumen** (S) + borrar huérfanos (15 min) + marcar #18 ✅.
2. Después: **Grabaciones premium** (waveform — gran demo) o **#13 Quality Rating**.
